{
  "name": "tekt-md-site",
  "version": "1.0.0",
  "description": "tekt.md static site",
  "scripts": {
    "profile": "node scripts/sync-profile.js",
    "profile:check": "node scripts/sync-profile.js --check",
    "prebuild": "npm run profile",
    "build": "eleventy",
    "start": "npm run profile && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "js-yaml": "^4.3.2"
  }
}
